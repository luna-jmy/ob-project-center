# 2026-09-20 工作日志

> 本轮（双 Tab / 时间粒度 / 逐项目配色 / 拖动排序 / 一键展开收起）的**详细记录写在
> `2026-09-18.md` 末尾**（该文件在 09-18 会话里建立，跨天的后续修订都续写在里面，
> 按标题「M3 修订 N」索引）。此文件只做检索入口，避免同一份内容两处维护。

## 本轮摘要（详见 2026-09-18.md「M3 修订 4」）
- commit `1a84ef4`：M3 全部内容（自绘甘特、全视图装配、左右联动、数据模型修正、年度筛选）
- commit `f53f073`：双 Tab（甘特 / Mermaid 预览+编辑栏）、Ctrl +/- 与 Ctrl+滚轮 时间粒度、
  逐项目配色、分组与组内拖动排序、一键展开收起、修右侧面板越界、修监听器累积
- 验证：tsc 0 错 / eslint 0 error / vitest 275 全绿 / esbuild 生产构建通过
  （main.js 114.6KB，无 document.body / innerHTML / !important）；
  styles.css 183 条规则全部 `.pm-` 前缀、0 `!important`

## 待用户实机确认
- Mermaid Tab 的预览（依赖宿主 MarkdownRenderer + 内置 mermaid）
- Ctrl +/- 与 Ctrl+滚轮 的粒度切换手感（普通滚轮保持滚动）
- 拖动排序在触屏上的表现
- folder 分组模式下「一分节一项目」时甘特分节头是否偏多
