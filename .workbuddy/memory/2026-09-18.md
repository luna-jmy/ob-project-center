# 2026-09-18 工作日志

## 文档重构（对齐 obsidian-plugin-development 技能）
- Luna 安装了插件开发技能（政策核验 2026-09-18），据此完成：
  - `agent.md` 瘦身：删除与技能重复的通用规则（生命周期/样式/文件 API/上架清单），改为「技能出处索引表 + 项目专属约束 6 条」（甘特库隔离、CSS 片段兼容、gantt-builder 共存、数据规则冻结、参数分离、纯本地红线）。
  - `SPEC.md` 对齐技能：状态改「已确认，进入构建」；§6 补 onunload 不 detach leaves、registerView 工厂、checkCallback 无副作用、popout ownerDocument、构建产物复查；§7 补依赖图验证后才声明 isDesktopOnly；§8 补易破坏数据边界测试（迁移可重复、标记块写入容错）；§9 上架改为现行 dashboard 流程、name Basic Latin 规则、tag 不加 v 前缀、versions.json 可选。

## 项目骨架（Milestone 0）
- git init，文档基线 commit 691c732。
- 脚手架：package.json（typescript/esbuild/vitest/eslint-plugin-obsidianmd/obsidian types）、tsconfig、esbuild.config.mjs（外置 obsidian/CM 依赖）、manifest.json（id=project-master, minAppVersion 1.8.7）、src/main.ts 薄装配（registerView/ribbon/command/onLayoutReady 占位）、src/types.ts（FieldMappingConfig+DEFAULT_SETTINGS+STATUS_CHINESE_ALIASES，全部对应 SPEC §5）、src/views/dashboard-view.ts（pm-dashboard-view 骨架）、src/settings.ts 占位、styles.css（pm- 前缀+CSS 变量）、tests/settings.test.ts 冒烟（字段映射对齐模板）、LICENSE(MIT)、README、.gitignore。
- 环境坑：bash shim 的 coreutils（cat/head/tail/dirname）损坏，npm shebang 也挂；解法=git 可直接用，npm 用 node 直调 npm-cli.js。官方 registry 连通 OK。
