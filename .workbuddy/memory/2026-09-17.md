# 2026-09-17 工作日志

## Obsidian 插件开发规范 agent.md
- 基于官方 docs.obsidian.md（Submission requirements / Developer policies / OO 自检清单 / eslint-plugin-obsidianmd）生成 agent.md，置于工作区根目录 `D:\WorkBuddy\obsidian-projectmaster\agent.md`。
- 重点：第 2 节冲突防护为一票否决（CSS 前缀隔离、禁内联样式/!important、register* 自动清理、禁 monkey-patch、Vault.process 原子写）。
- 第 6 节固化上架规范：manifest id 禁含 obsidian、SemVer、Release tag==version、README 披露、禁客户端遥测/自更新。

## Project Dashboard Gantt 插件 SPEC
- 走 superpowers 流程阶段 1，产出 `D:\WorkBuddy\obsidian-projectmaster\SPEC.md`（待用户确认）。
- 已确认决策：数据源=笔记 frontmatter（字段可重映射）；MVP=甘特图核心（跳转+UI编辑）+筛选排序+分组面板，可视化/代码块嵌入进 v2；UI=独立 ItemView；渲染=frappe-gantt + 隔离适配层（对齐 agent.md 冲突防护）。
- 架构：metadataCache 增量索引 + filter-service 纯函数管道 + processFrontMatter 单向写回。
- 下一步：确认后开 worktree → 写实施计划（writing-plans）。

## SPEC 重写（基于真实参考）
- Luna 提供参考：ref/TPL-Project.md（项目模板 frontmatter：type/status 7枚举含中文别名/priority 1-5/start_date/due_date/end_date fallback/progress/area/objective/long-term/main-project 等）、ref/项目中心（升级）.md、ref/projectGantt.js（objective 分组甘特）、ref/projectOverview.js（筛选栏+快速项目/main-project 代表逻辑/日期区间交集规则）。
- 核心痛点（插件立身之本）：①mermaid 甘特静态渲染点不动，无法跳转笔记；②筛选绑死 dashboard 笔记 frontmatter 传参、限制太多。
- SPEC 已重写：数据模型零迁移沿用 TPL 字段体系；F1 交互甘特+联动一等公民；F2 筛选收进视图 UI 改多选自由组合（status chips、area 直接多选、防抖搜索、日期预设+自定义）；继承业务规则清单（±7天兜底、long-term 豁免、cancelled 排除、main-project 代表）；gantt-builder（task→mermaid 插件）v1 共存不碰正文标记块，v2 整合。
- 自研插件 gantt-builder 为 Luna 已开发插件，模板内有 %% gantt-builder:start %% / gantt-builder-data-start %% 标记块。

## SPEC 补充：Mermaid 导出
- Luna 新要求：甘特弃用静态 mermaid、插件交互式重建，但必须支持导出 mermaid 代码便于笔记引用。
- 已写入 SPEC F1.7：复制 Mermaid（按当前筛选/分组/排序状态导出，格式逐条对齐 projectGantt.js 约定：dateFormat/axisFormat/objective 分 section/done-active 标记/任务名清洗）；增强项「导出到笔记」写入 %% gantt-builder:start %% 标记块（v2 整合衔接点）；导出器为纯函数全量单测；数据单向（mermaid 只是导出目标，事实源仍是 frontmatter）。

## SPEC 补充：参数分离设计（§5 重写）
- Luna 要求：插件化后做参数分离，全局参数含项目文件夹位置等；模板自定义字段也要参数分离。
- §5 重写为四组：①全局参数（扫描目录改多目录、快速项目路径标记、排除文件夹、中文别名开关、日期兜底策略）；②字段映射参数⭐（TPL-Project 每个业务字段→逻辑字段 ProjectItem，映射表可导出/导入 JSON，读写/导出/Modal 全走映射层）；③枚举与展示参数（status 别名表、priority emoji 映射、suggester 顺序）；④视图偏好。约束：settings version+迁移、loadData/saveData、原生设置 API。
