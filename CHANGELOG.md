# Changelog

本项目的所有重要改动都记录在这里。

格式参考 [Keep a Changelog](https://keepachangelog.com/zh-CN/1.1.0/)，
版本号遵循[语义化版本](https://semver.org/lang/zh-CN/)。

## [Unreleased]

## [1.2.0] - 2026-09-21

### 新增

- **界面语言**：新增字典与「界面语言」设置（跟随 Obsidian / 中文 / English）。
  界面文案全部经 `t()` 取当前语言——**键就是中文原文**，漏翻自动退回中文，
  界面上永远不会露出生 key。
  注意：扫描目录、快速项目标记、资料子文件夹名这类**数据默认值不随语言变**，
  它们是 vault 里的既有约定，切语言不该让它们跟着变
- 命令名与 ribbon 名称也纳入字典（此前写死英文，中文用户看到的反而是英文）
- **侧栏宽度可拖动**：侧栏与主区之间新增分隔条（鼠标拖动，方向键 ±16px、
  按住 Shift ±64px），宽度记进设置、重开后仍在；侧栏收起或进入面板模式时分隔条自动让位
- 新建项目弹窗的「目标 / 领域」改为**与编辑界面同一套输入**（可输入的下拉：能选已有的、
  也能直接打新的；候选值来自库里已经写过的值），两处共用同一份实现，
  于是新建时就不必手打出「市场部」这种与「市场」并存的变体

### 变更

- **「领域」改成单值字段**（用户口径 2026-09-21）：它是分组维度，多值会让分组失效——
  分组只认第一个值，其余值在分组里根本看不见，而筛选按任意值匹配，于是同一个项目
  「筛选能筛到、分组里找不到」。编辑与新建弹窗里的领域都改成可输入的下拉（与「目标」
  同一套）。老笔记里的 `area: [a, b]` 读取时**取第一个**：与旧分组行为完全一致，
  所以这些项目不会突然从某个分组掉进「未设置」

- 文案层从「模块级常量映射」改为按当前语言求值的函数
  （`statusLabel()` / `priorityLabel()` / `fieldCopy()` / `barColorCopy()` /
  `ganttSkipMessage()` / `barColorPresets()` / `statusPresetLabel()` /
  `zoomLabel()` / `tabLabel()`）：常量映射会在 import 时把语言冻住，切了语言界面不跟着变

### 修复

- 按**目标 / 领域**分组时，甘特图上只剩一个分节就不画分节名，而左侧面板一直显示——
  看起来就是「面板分组了、甘特没分组」（用户报 2026-09-21）。值分组现在始终显示分节名
  （那标题就是信息本身）；folder / 不分组维持原口径，单节标题常与组内项目重名，是噪声
- 设置页「默认排序」与筛选栏**逐档对齐**：筛选栏有的「开始日 ↑/↓」「截止日 ↓」
  在此选不到，现在两边档位一样多、措辞也一致；「年」档按它实际画的刻度改称「季度」。
  `manual` 仍不进设置页——它靠拖动自动切换，不是用户挑的档位

### 测试

- 新增 `tests/i18n.test.ts`：翻译回退、占位符替换、语言解析，外加两条**覆盖守卫**——
  扫源码里所有 `t("…")` 的中文、并从取值函数入口取中文，逐个查英文字典，漏翻直接红
  （这次领域字段的说明文案就是它抓出来的）
- 新增 `tests/split-resizer.test.ts`（宽度收敛：越界收敛而非丢弃、非法值给下限、
  上限低于下限时不返回自相矛盾的值）与 `tests/suggestion-fields.test.ts`
  （多值字段标签的点选语义，含「市场」与「市场部」不互相匹配）
- 新增「老笔记多值领域取第一个」的回归测试（`tests/project-item.test.ts`）
- `vitest`：25 个测试文件 / 485 例全绿；`tsc` 与 `eslint`（官方预设）零错误

## [1.1.0] - 2026-09-21

### 新增

- 排序档新增「开始日 ↑」「开始日 ↓」「截止日 ↓」
- **面板模式**下可以拖动调整顺序：不分组时拖**卡片**（抓手在卡片左上角），
  分组时拖**分组卡片**（抓手在分组头最左侧）——与甘特侧栏共用同一套手动排序落盘
- 不分组 + 时间排序时，**长期项目置顶**（它们没有时间边界，不该沉到列表尾）；
  侧边栏与面板模式行为一致
- 「不会出现在甘特图上」的项目卡片（长期 / 缺日期）用底色区分，原因悬停可见
- 「面板模式」按钮改为强调色样式并带激活态，与普通工具栏按钮区分
- 新增设置项**「新建项目模板」**：留空与原来一致；填了路径则正文整篇用模板，
  只补模板还缺的 frontmatter，模板里的 Templater 命令（`<% %>`）与
  核心「模板」插件的日期占位符（`{{date:…}}`）都会被处理
- 面板卡片与紧凑行新增**「编辑」按钮**：长期项目按设计不上甘特图，
  这是它们在两种视图下唯一的界面入口
- 新建项目弹窗的「上级目录」改为**扫描目录下拉 + 可选子文件夹**，不必重打已配过的路径
- Mermaid 预览新增两个图片出口：**导出 SVG** / **导出 JPG**（存到 Obsidian 的
  「附件默认位置」，文件名带时间戳、同名自动加序号；JPG 为 2 倍分辨率、底色跟随主题）。
  导出后**自动把文件路径复制到剪贴板**——附件目录文件多，有路径才好定位

### 变更

- **取消的项目照常出现在甘特图上**；移除设置项「甘特图隐藏已取消项目」——
  要不要显示取消的项目由状态筛选决定（筛选里本来就有「取消」这个可选项），
  同一件事不再留两个开关
- **「不分组」不再按「有资料 / 没资料」切**：除快速项目单独成区外，
  所有项目合成一份扁平列表，一个项目一张卡片
- **「资料子文件夹名」与「快速项目路径标记」都允许留空**：留空 = 资料与项目文档
  放在同一个文件夹 / 快速项目直接放在扫描目录下（资料归集本来就同时收同层与子文件夹）
- 新建的快速项目落到「快速项目」标记文件夹里（缺该文件夹自动新建），
  与读取侧的快速项目判定同一口径

### 修复

- 状态档选「全部」时，状态无法识别（frontmatter 里非法/缺失）的项目也会显示——
  此前它们会被静默排除，且在界面上无从修复
- 编辑保存后，如果项目因筛选或甘特规则（长期 / 缺日期）而不可见，
  会明确提示原因与解决方法，不再无声消失
- 状态档下拉文案如实写出隐藏范围：「隐藏已完成」实际还隐藏「取消」与「归档」
- 「资料子文件夹名」「快速项目路径标记」删空后重启会变回半截值
  （逐字符写入 + 空值被忽略，删到中间态时那个值已经落盘）
- 新建项目弹窗的落点提示与实际创建位置不一致
- 模板路径按 Obsidian「复制路径」的形态容错：缺 `.md` / 绝对路径 / `[[wikilink]]` / 引号
- 面板拖动排序：落点改用幽灵自身位置 + 阅读顺序判定，不再整体偏移一格
  （原先按指针位置判定，而抓手在卡片左上角）

### 测试

- `vitest`：22 个测试文件 / 460 例全绿；`tsc` 与 `eslint`（官方预设）零错误
- 新增 `tests/svg-image.test.ts`（导出文件名、附件目录推导、viewBox 解析）
- 图片导出与拖动排序属 DOM 层，仓库对 panels 不写 DOM 测试——这两块按要求在
  Obsidian 里实机验收，未用自动化测试冒充覆盖

## [1.0.0] - 2026-09-20

首个可用版本。数据源是项目笔记的 frontmatter，识别方式与字段映射均可配置，
现有 TPL-Project 模板的项目笔记不需要任何迁移即可被识别。

### 新增

- **交互式甘特图**（自绘 SVG，无运行时依赖）：点击跳转笔记、右键菜单换色与编辑、
  `F2` / `ContextMenu` 键盘入口、拖动改期（落盘前可 `Esc` 回滚）、
  时间粒度三级缩放（默认档 + 快捷键命令 + `Ctrl` + 滚轮）
- **筛选栏**：status 多选、area 包含/排除、名称搜索、年度筛选、
  日期区间（预设 + 自定义 + 清除），以及「隐藏已完成」「进行中」等预设
- **分组面板**：按文件夹 / 目标 / 领域 / 不分组四种维度；快速项目分区固定最前；
  带资料的项目 → 独立卡片（进度条 + 资料清单），无资料的 → 紧凑列表
- **左右联动**：两侧吃同一份分组结果，双向悬停高亮、分节折叠同步、点击定位
- **新建与编辑**：新建项目支持「带文件夹」「快速项目」两种形态；
  编辑 Modal 覆盖状态、优先级、起止日期、进度、目标、领域、负责人/成员、长期与代表标记
- **新建项目模板联动**：正文用模板、只补模板缺的 frontmatter 字段；
  模板里的 Templater 命令交给 Templater 执行，核心模板语法 `{{date:…}}` 自行替换；
  模板路径容忍绝对路径 / 缺扩展名 / wikilink 三种复制形态
- **Mermaid 导出**：复制到剪贴板、导出到笔记的标记块（原子写入、基于最新内容最小改动）、
  手改即进草稿态、排除周末与指定日期
- **法定节假日排期**：按年维护「放假」与「调休上班」，导出时按甘特图跨越的年份自动套用
- **甘特条配色**：进行中 / 已完成 / 关键任务 / 其他四类可配，
  并支持逐项目自定义颜色（非法值报数据问题，不静默退回）
- **手工排序**：拖动分组与组内项目，拖动即切到「手动」排序档
- **设置页**：六个 tab（全局参数 / 字段映射 / 枚举与展示 / 视图偏好 / Mermaid 导出 /
  法定节假日排期）；字段映射、status 别名、emoji、suggester 顺序全部可配，
  映射表支持 JSON 导入导出
- 面板卡片「编辑」按钮：长期项目按设计不进甘特图，面板是它唯一的界面入口
- 面板卡片字号缩放（60% – 220%）

### 变更

- 新建的**快速项目**改放进「快速项目」文件夹（缺则现建），不再直接落在扫描目录根层
- 新建项目的**上级目录**改为「扫描目录下拉 + 可选子文件夹」，不再要求手打全路径
- 设置结构版本 4 → 5；迁移函数保持全函数、逐字段降级、可重复执行

### 修复

- 深色主题下下拉框、输入框、胶囊按钮的配色（改用主题的表单与按钮面变量）
- 逐项目自定义颜色不再经 CSS 变量中转 —— 此前设了颜色的甘特条会渲染成黑色
- Windows 深色模式下原生 `<select>` 选中文字的重影（显式关闭原生外观）
- priority 1 / 2 的项目导出为 mermaid 的 `crit` 关键任务标记
- 设置项允许留空：「资料子文件夹名」「快速项目路径标记」此前无法清空
  （逐字符写入时把删到一半的中间态存了下来）
- 点面板里的长期项目不再误报「缺起止日期」，改为正确原因并指向编辑入口
- 资料归集口径：递归子文件夹、同层的其他项目文档不算资料、别人的地盘整棵剪掉
- 右侧面板越界与刷新时的监听器累积

### 测试

- `vitest`：21 个测试文件 / 437 例全绿；`tsc` 与 `eslint`（官方预设）零错误
- 纯逻辑层全覆盖：规范化、筛选管道、分组与资料归集、甘特模型与时间刻度、
  Mermaid 导出、字段映射、设置迁移、节假日排期

[Unreleased]: https://github.com/luna-jmy/ob-project-center/compare/1.1.0...HEAD
[1.1.0]: https://github.com/luna-jmy/ob-project-center/releases/tag/1.1.0
[1.0.0]: https://github.com/luna-jmy/ob-project-center/releases/tag/1.0.0
