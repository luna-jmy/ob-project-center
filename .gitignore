# 构建产物
#
# `main.js` 是**提交进仓库**的（用户口径 2026-09-22）：本仓库没有 CI 打包流程，
# 而 Obsidian 只认仓库根目录的 main.js —— 忽略它，别人 clone / 同步下来的就是一个
# 装不上的插件。它由 `npm run build` 生成，改完源码记得重新构建并提交。
# 只有 sourcemap 与打包用的 zip 不进版本库。
*.map

# Dependencies
node_modules/

# Editor
.vscode/
.idea/

# OS
.DS_Store
Thumbs.db

# 私有内容：本地工作记录、设计规格与开发约束，不进版本库
# （已从索引中移除，见 git rm --cached；文件仍保留在本地）
.workbuddy/
SPEC.md
agent.md
AGENTS.md

# 发布产物：dist/ 放打包好的 zip（npm run package），只作 Release 附件，不进版本库
dist/
